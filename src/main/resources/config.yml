discord:
  token: ""
  status: "ONLINE"
  activity:
    type: "PLAYING"
    game: "%player_count% players in server"
    url: ""

server-name: "server"

chat-max-length: 150 # 0 = unlimited
chat-max-lines: 0 # 0 = unlimited

discord-souce-name: "Dis"

system-channel: 0

channels:
  global: 0

role-prefix:
  default: "%role_color%"
